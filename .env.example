# Copy to .env and fill in. .env is gitignored; never commit it.
#
#   cp .env.example .env
#
# SEC fair-access guidance asks for a contact address in the
# User-Agent. Requests without one are refused with HTTP 403.
SEC_CONTACT_EMAIL=
